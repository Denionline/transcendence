services:
  database:
    build: ./database
    image: trans_db
    container_name: transcendence-db
    env_file: ../.env
    ports:
      - "${POSTGRES_HOST_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - matosinhos

  backend:
    build:
      context: ./backend
    image: trans_be
    container_name: express_backend
    env_file: ../.env
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@database:5432/${POSTGRES_DB}?schema=public
      PORT: 9000
      UPLOAD_DIR: /app/uploads
    volumes:
      - uploads:/app/uploads
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "node -e \"fetch('http://localhost:9000/health').then(r => r.json()).then(d => process.exit(d.status === 'ok' ? 0 : 1)).catch(() => process.exit(1))\"",
        ]
    depends_on:
      database:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - matosinhos

  frontend:
    build:
      context: ./frontend
    image: trans-fe
    container_name: react_frontend
    env_file: ../.env
    environment:
      HTTPS_PORT: "${HTTPS_PORT:-8443}"
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://localhost:5173').then(() => process.exit(0)).catch(() => process.exit(1))\""]
      interval: 30s
      timeout: 10s
      start_period: 15s
      retries: 3
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - matosinhos

  nginx:
    build:
      context: ./nginx
    image: trans_proxy
    container_name: nginx_proxy
    environment:
      NGINX_ENVSUBST_FILTER: "^NGINX_"
      NGINX_HTTPS_PORT: "${HTTPS_PORT:-8443}"
      NGINX_MAX_BODY_SIZE: "${MAX_UPLOAD_MB:-50}m"
    ports:
      - "${HTTP_PORT:-8080}:80"
      - "${HTTPS_PORT:-8443}:443"
    healthcheck:
      test: ["CMD-SHELL", "wget --no-check-certificate -qO- https://127.0.0.1/health || exit 1"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - matosinhos

volumes:
  pgdata:
  uploads:

networks:
  matosinhos:
    name: matosinhos
    driver: bridge
